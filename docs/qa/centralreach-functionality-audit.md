# CentralReach Functionality Audit — Phase 1

Audit generated by inspecting the live database, canonical view, RPCs, hooks,
and route consumers. Numbers below come from real `SELECT` counts (not mocks).

**Snapshot date:** 2026-07-21
**Scope:** Every non-dashboard surface that reads CentralReach-derived data
across Clinical, RBT, BCBA, and Reports.

---

## 1. Ground truth: what is actually imported

| Source table / view | Row count | Freshness | Notes |
| --- | ---: | --- | --- |
| `bcba_productivity_billing_rows` | **47,533** | 1 batch (2026-01-01 → 2026-06-30) | Only populated CR import. Powers `v_cr_canonical_sessions`. |
| `bcba_productivity_upload_batches` | 1 | latest 2026 batch | Single active import batch. |
| `v_cr_canonical_sessions` | 47,533 | derived | Canonical row-shape used by every reporting adapter. |
| `shared_report_datasets` (active) | 4 | 2026-07-15 | Authorization + 3 cancellation CSVs. No `bcba-productivity` file dataset — that report is fed by billing rows. |
| `bcba_productivity_snapshots` | **0** | — | Aggregation table never materialized. |
| `bcba_billable_sessions` | **0** | — | Legacy; no consumer relies on it. |
| `bcba_billable_imports` | **0** | — | Legacy. |
| `cr_sync_runs` / `cr_external_records` | 0 / 0 | — | New CR Sync engine not yet emitting; `cr_sync_freshness` returns empty. |
| `clients` (public) | **0** | — | No CR-derived clients promoted into `public.clients`. Client-list surfaces cannot query this table for real data today. |

> **Phase 1a P0-1 status (2026-07-21):** `public.clients` now contains **879**
> rows keyed on `centralreach_id`, promoted from `v_cr_canonical_sessions` via
> the new admin-only `promote_canonical_clients()` RPC. The idempotent
> `resolve_client_by_cr_id(text)` RPC and the front-end
> `canonicalClientResolver` module let any Clinical/BCBA/RBT surface translate
> a CentralReach id into the internal client UUID. A new `/clients/cr/:crId`
> alias route forwards CR-only deep links to the canonical detail page.
| `employees` | 366 | current | 31 linked to `auth.users` (`user_id`); **0 with `centralreach_id`**. |

### `v_cr_canonical_sessions` breakdown

| session_kind | rows |
| --- | ---: |
| direct | 31,755 |
| supervision | 7,885 |
| assessment | 4,658 |
| parent_training | 1,614 |
| admin | 1,348 |
| cancellation | 270 |
| other | 3 |

Coverage: 546 distinct `cr_provider_id`, 879 distinct `cr_client_id`,
`service_date` range **2026-01-01 → 2026-06-30**.

### Provider identity mapping (P0)

| provider_mapping_status | rows | % of canonical |
| --- | ---: | ---: |
| mapped (employee resolved) | 20,310 | 42.7% |
| **unmapped** | 27,223 | **57.3%** |

- Distinct employees currently receiving any canonical attribution: **128 / 366**.
- Employees with `centralreach_id` set: **0 / 366** — every mapping today is
  name-fallback via `v_cr_provider_mapping`.
- `employees.user_id` populated: **31 / 366** — RBT/BCBA auth-scoped queries
  can only resolve for those 31 accounts.

---

## 2. Canonical pipeline map

```
bcba_productivity_billing_rows (47,533)
       │  normalized by
       ▼
v_cr_canonical_sessions ── joins ──▶ v_cr_provider_mapping ──▶ employees.id / auth.users.id
       │
       ├── canonical_sessions_provider_summary() RPC
       ├── canonical_sessions_client_summary() RPC
       ├── canonical_sessions_rows() RPC
       ├── canonical_sessions_unmapped_providers() RPC
       ├── canonical_report_billing_rows() RPC
       ├── canonical_report_client_hours() RPC
       ├── canonical_report_provider_hours() RPC
       ├── canonical_report_totals() RPC
       └── report_source_coverage() RPC

shared_report_datasets (4)  ──▶ Authorization + Cancellation report engines
```

All eight canonical RPCs exist in Postgres (verified via `pg_proc`).
`cr_sync_freshness()` exists but returns empty because `cr_sync_runs` is empty
— `useCrSync` falls back to legacy `rbt_data_sync_status` per its comment.

---

## 3. Consumer inventory

### BCBA surfaces

| Page | Hook | Source of truth today | Status |
| --- | --- | --- | --- |
| Caseload (`/bcba/caseload`) | `useCaseload` | canonical client summary + `bcba_action_tasks` | OK for BCBAs whose auth uid resolves to `provider_auth_user_id`; empty for BCBAs whose employee row lacks `user_id`. |
| Supervision (`/bcba/supervision`) | `useSupervisionMonth` | canonical `session_kind='supervision'` | OK |
| Parent Training (`/bcba/parent-training`) | `useParentTraining` | canonical `session_kind='parent_training'` | OK |
| Productivity (`/bcba/productivity`) | `useProductivity` | canonical provider summary | OK |
| Home (`/bcba/home`) | `useBcbaHomeData` | canonical provider summary + tasks | OK |

### RBT surfaces

| Page | Hook | Source | Status |
| --- | --- | --- | --- |
| RBT Home cockpit (`/rbt/app/home`) | `useCanonicalProviderSummary` via `homeCockpit` | canonical | Works only for the 31 RBT accounts auth-linked; others correctly see the Program Setup fallback. |
| RBT Schedule | `useRbtReportSummaries` | canonical | OK |
| RBT Clients list | canonical role bridge | canonical | RBT client detail routes that expect a `clients.id` row will 404 for imported CR clients — `public.clients` is empty. |
| RBT Performance | canonical | OK |
| RBT Credentials | `rbt_credentials` | out of scope for CR |

### Clinical / Clinical Leadership surfaces

| Command Center page | Data path | Status |
| --- | --- | --- |
| Caseload / provider drilldowns | canonical provider + client summary | OK for aggregates; **client drilldowns render "client not found" for canonical clients — P0.** |
| Authorization visibility | `shared_report_datasets['authorization']` (CSV parsed client-side) | OK when CSV present. |
| Service utilization | canonical `direct` / `supervision` | OK |

### Reports (`/reports`)

| Route | Backing source | Verified live? |
| --- | --- | --- |
| `BcbaProductivityReport.tsx` (legacy V1) | direct `bcba_productivity_billing_rows` | Superseded — should redirect to V3. **P1** |
| `BcbaProductivityReportV3.tsx` | canonical RPCs | OK — 47k rows render |
| `HrBcbaProductivityDashboard.tsx` | canonical provider summary | OK |
| `QaSupervisionPtDashboard.tsx` | canonical (`supervision`, `parent_training`) | OK |
| `QaAuthUtilizationDashboard.tsx` | `shared_report_datasets['authorization']` | OK when CSV present |
| `QaCancellationDashboard.tsx` | `shared_report_datasets['cancellation-*']` | OK when CSVs present |
| `CancellationCommandCenter.tsx` | canonical + shared | OK |
| `HrEmployeeComplianceDashboard.tsx` | `employees` + `employee_trainings` | Not CR-fed |
| `HrEmployeeOnboardingCommandCenter.tsx` | `employee_onboarding` | Not CR-fed |
| `HrPayrollCommandCenter.tsx` | Viventium (`payroll_*`) | Not CR-fed |
| `HrRecruitingPipelineDashboard.tsx` | `recruiting_*` | Not CR-fed |
| `ReportDetail` / `ReportsLanding` / `ReportsHome` | catalog + `report_source_coverage()` | OK freshness badges |

---

## 4. Gaps ranked

### P0 — blocks real users today

1. **Empty `public.clients`.** Clinical/BCBA/RBT client drilldowns cannot load
   a client detail row for any CR client because promotion from
   `v_cr_canonical_sessions` into `public.clients` has never happened.
   *Fix path:* migration that back-fills `public.clients` from distinct
   `(cr_client_id, client_name)` in canonical rows, keyed on a new
   `centralreach_id text unique` column; expose an idempotent
   `promote_canonical_clients()` RPC used by future batches.
2. **Provider identity: 57% of canonical rows unmapped.** 27,223 rows / 418 CR
   provider IDs have no matching employee; `employees.centralreach_id` is
   `NULL` across the board.
   *Fix path:* run existing `reconcile_employee_centralreach_ids()` RPC,
   surface the 418 unmatched IDs in `CentralReachHub` for manual pairing that
   writes `employees.centralreach_id`.
3. **Employee ↔ auth linkage: 335/366 employees have no `user_id`.** BCBA/RBT
   role-scoped queries filtering on `auth.uid()` return empty for those users.
   This is the biggest single reason real users open a page and see nothing.
   *Fix path:* admin RPC that back-fills `employees.user_id` from
   `auth.users.email`, exposing unresolved rows in User Management.

### P1 — degrades trust but not blocking

4. Legacy `BcbaProductivityReport` (V1) still routable — redirect to V3.
5. `bcba_productivity_snapshots` defined but never materialized. Either drop
   the table + hooks or populate via a trigger on batch upload.
6. `cr_sync_runs` empty → `cr_sync_freshness()` empty → banners fall back to
   legacy `rbt_data_sync_status`. Wire productivity upload flow to write a
   `cr_sync_runs` row on success.

### P2 — hygiene

7. Drop `bcba_billable_sessions` / `bcba_billable_imports` (0 rows, no consumers).
8. Enforce single active dataset per `report_key` in
   `shared_report_datasets` (partial-unique index on `is_active=true`).
9. Enforce `provider_mapping_confidence >= 0.8` filter on personal role
   dashboards so low-confidence name matches don't leak into BCBA/RBT views.

---

## 5. Evidence commands (reproducible)

```sql
SELECT count(*) FROM v_cr_canonical_sessions;                       -- 47533
SELECT provider_mapping_status, count(*) FROM v_cr_canonical_sessions
  GROUP BY 1;                                                       -- 20310 / 27223
SELECT count(*) FROM public.clients;                                -- 0
SELECT count(*) FILTER (WHERE user_id IS NOT NULL), count(*)
  FROM public.employees;                                            -- 31 / 366
SELECT count(*) FILTER (WHERE centralreach_id IS NOT NULL)
  FROM public.employees;                                            -- 0
SELECT report_key, uploaded_at FROM shared_report_datasets
  WHERE is_active;                                                  -- 4 rows
```

---

## 6. Phase 1 status

- **Step 1 (audit + inventory):** complete — this document.
- **Steps 2–5 (pipeline repair, fallback removal, end-to-end wiring, tests):**
  not implemented in this session. They depend on three coordinated
  migrations (client promotion, employee-auth backfill, employee-CR pairing)
  plus role-scoped query updates across ~20 hooks. Splitting into follow-up
  passes prevents landing half-migrated schema in a single commit.
- Nothing published.
